## photos项目

一款**纯本地离线、高性能、高精度**的证件照全自动处理工具，基于 Rust 技术栈实现核心业务，融合 LiYing 经典姿态纠偏算法与业界顶级 BiRefNet 高精度抠图模型。



### 功能

- **三模式傻瓜式模型套件**，用户无需手动选择模型，一键切换「极速/CPU高性能/GPU高质量」，同时支持专业自定义配置。

- 双运行模式：**CLI命令行批量处理 + Tauri桌面WebUI可视化操作**。

- Tauri2+React19+Axum 现代化桌面架构

- 集成 BiRefNet/RMBG/RetinaFace/MoveNet 顶级模型，支持智能套件自动适配。`项目根目录\models`存储模型文件

- TOML 统一配置管理: 全局配置，预定义三套模型套件、图像参数、排版参数

  - 三套预设模型套件: **屏蔽底层模型细节，用户仅选择3种场景模式，系统自动加载对应模型组合，彻底解决小白选型困难**

    - #### 极速模式（Speed）

      适配低配电脑、批量高速处理，牺牲极致画质换取速度

      - 人脸检测：MTCNN
      - 人体关键点：MoveNet-Lightning
      - 人像分割：RMBG-1.4/2.0
      - 适用场景：日常快速出图、低配设备、大批量流水线处理

    - ####  CPU高性能模式（Balanced·默认模式）

      CPU设备最优平衡方案，画质与速度均衡，日常使用首选

      - 人脸检测：RetinaFace（高精度人脸定位）
      - 人体关键点：MoveNet-Lightning
      - 人像分割：BiRefNet-Lite（轻量化高精度发丝抠图）
      - 适用场景：绝大多数用户日常证件照制作，默认启动模式

    - #### GPU高质量模式（Quality）

      N卡CUDA加速，极致画质，发丝细节拉满

      - 人脸检测：RetinaFace
      - 人体关键点：MoveNet-Thunder（高精度姿态识别）
      - 人像分割：BiRefNet-Full（完整版顶级抠图模型）
      - 适用场景：证件照精修、报名照、签证照、高清出图

- pipeline 流水线: **图像输入 → 人脸+人体关键点检测 → 融合姿态角度计算 → AI人像抠图生成Mask → 原图+Mask同步仿射纠偏 → Alpha背景替换 → 美颜后处理（可选） → 标准尺寸裁切 → 智能排版 → 结果输出**



#### 智能姿态纠偏模块（核心独家能力）

- 复刻 LiYing 核心算法：**头部姿态角 + 双肩水平线角度加权融合**（权重：头部0.6、肩部0.4）
- 角度阈值保护：最大自动纠偏角度 ±22°，超阈值拒绝纠偏并提示用户
- 解决普通工具无法修复的高低肩、侧身倾斜、头部歪斜问题
- 支持自动水平校正、画面居中矫正



####  AI智能抠图模块

- 支持多模型推理：BiRefNet Full/Lite、RMBG-1.4、MODNet、可扩展新增模型
- 发丝级高精度抠图，边缘羽化降噪，杜绝白边、黑边、漏边
- 支持透明背景、纯色背景、自定义背景图替换



#### 图像后处理模块

- 智能换底色：支持RGB自定义色值、常用红白蓝证件底色一键切换
- AI美颜：磨皮、美白、提亮、肤色均衡（可开关、强度可调）
  - 纯 Rust 传统图像算法磨皮调色（双边滤波等自研算子）
  - AI 美颜 ONNX 模型（可选扩展，默认不启用）
- 智能换装：正装替换，适配证件照规范。 人像解析 + 虚拟试衣模型



### 运行模式需求规范

#### CLI 命令行模式

- 支持单图处理、文件夹批量处理
- 支持指定运行模式（speed/balanced/quality）
- 支持自定义尺寸、底色、排版、压缩参数
- 无GUI依赖，适配服务器、自动化脚本、批量运维场景

#### WebUI桌面模式（Tauri2+React19）

- 本地桌面窗口运行，无需浏览器独立打开
- 界面极简：仅展示3种运行模式、图片上传、预览、导出、高级设置
- 支持实时预览纠偏、换底、美颜效果
- Axum 后台静默提供接口，前端无感知调用核心流水线
- 支持批量上传、批量处理、打包下载




### 技术栈

- 4层**DDD 架构（领域驱动设计）**和**TDD（测试驱动开发）**设计思想
- 优先使用最新稳定版本
- Rust 
- clap（CLI 参数解析）
- Axum（HTTP接口服务）
- Tauri 2.0
- ONNX Runtime 推理模型（CPU/CUDA 自适应）
- **图像处理**：`image` + `imageproc` + `nalgebra` + 自研算子（旋转、仿射、融合、裁切、排版等；弃用 OpenCV-RS，见 `docs/adr/0001-纯Rust图像栈.md`）
- TypeScript，React 19.2+，Tailwind CSS，Zustand；**本阶段不引入 ECharts**
- [按需可选]中文字体内嵌（SIL 协议，无版权风险）：Noto Sans SC + 系统默认字体（保底策略）
- [按需可选]统一入口运行配置文件：`application.toml`. 参数优先级: 命令行参数 > 环境变量 > toml 配置文件 > 默认值



### 文档和注释

- 项目文档统一存储docs目录，按顺序标号命名中文文档
- 注释、日志内容统一使用中文，使用 UTF-8 无 BOM 编码
- 统一错误处理，所有错误信息都必须使用中文，禁止打印明文密码等敏感信息
- 数据默认保存`data`目录下，使用sqlite(wal模式)持久化数据
- 及时更新文档
- SSH 推送github仓库（git@github.com:bliubiu/photos.git）



#### CHANGELOG 规范

> 版本号类型说明：**CalVer（日历版本）**/**SemVer（语义化版本）**
>
> - CalVer 版本号格式：YY.MM.DD.MICRO
> - SemVer 格式：`主版本号Major.次版本号Minor.补丁号Patch[-可选标签 alpha|beta|rc]`

- 使用CalVer（日历版本）版本号
- **版本规范格式**：`YYYY.MM.DD.MICRO`（年.月.日.迭代号）
- **CHANGELOG 标题格式**：`## [YYYY.MM.DD.MICRO] - SemVer`；其中 SemVer 后缀（如 0.1.0）为**兼容标注**，供包管理器与对外发布识别，**版本主口径一律以 CalVer 为准**
- 项目根目建立 CHANGELOG.md 文档，单个CHANGELOG.md 文档随时间线推移逐渐变得臃肿，定期归档到docs\changelogs目录
- 根据迭代内容，严格按照 SemVer/CalVer 规则确定新版本号
- 在稳定分支打 Tag，Tag 名称与版本号一致
- Tag 仅允许正式发布时创建，禁止随意打测试 Tag

##### 格式模板

```
## [2026.08.17.0] - 0.1.0
### ⚠️ Breaking Changes  破坏性变更
- 【用户模块】废弃旧版登录接口 /api/v1/login，请迁移至 /api/v2/login

### ✨ New Features 新增功能
- 【权限模块】新增角色批量授权功能 #123

### 🐛 Bug Fixes  问题修复
- 【订单模块】修复极端场景下订单状态同步异常问题 #121

### 📈 Improvements 性能/体验优化
- 【接口服务】优化列表查询性能，响应速度提升40%

### 📚 Docs 文档更新
- 更新权限模块使用文档与接口说明

### 🔧 Dependencies 依赖更新
- 

### 🗑 Deprecated 废弃功能
- 
```



### 注意事项

- 每次改动完成后，都必须创建一个对应的 Git commit，以便后续追踪和回滚。
- 每次改动后，都必须编写或更新相关测试，并在交付给用户前，确保所有测试和验证全部通过。



### 日志系统

支持多日志级别（DEBUG、INFO、ERROR），并提供文件输出和日志轮转功能。

- 日志级别：支持 DEBUG、INFO、ERROR 三个级别，可通过配置文件指定日志级别，默认 INFO 级别
- 日志格式：`[YYYY-MM-DD HH:MM:SS.SSS] [级别] [线程ID或进程ID] [模块:行号] - 日志内容`
- 日志输出：支持文件输出和终端输出，文件输出路径默认 `项目根目/logs/`，日志文件按日期轮转（每日一个文件），保留 32 天日志，自动清理过期日志
- 日志内容：统一使用中文，清晰记录操作行为、执行结果、错误信息，便于问题排查和审计
- 日志内容不能记录敏感信息，必须严格遵循脱敏策略，禁止记录：密码、完整身份证号、手机号
- 文件命名格式：`项目名-YYYYMMDD.log`，例如 `photos-20231225.log`
- 时间戳格式：YYYY-MM-DD HH:MM:SS.SSS  本地时间，不含时区

  


### 测试策略

- 使用TDD设计思想
- 单元测试、API 测试、集成测试、系统测试覆盖率100%



### 安全

[加密和脱敏](.agents\01-敏感数据加密与脱敏安全设计文档.md)



### 约束和禁止条件

- 依赖单向、边界清晰、pub 收敛

- 职责单一化、模块化
- 禁止引入任何 C/C++ 图像库绑定（含 OpenCV-RS；图像处理仅用纯 Rust 栈，见 ADR-0001）
- 禁用 npm 包管理，可用pnpm或bun
- 禁用Maven，可用gradle
- 使用uv管理python 软件包，禁止使用pip/venv/pipenv/poetry
- 必须编译配置优化，从源头减小体积



## 附录

### 参考文档

- https://github.com/aoguai/LiYing 
- https://github.com/Zeyi-Lin/HivisionIDPhotos
- rembg（RMBG 推理生态参考，非实现依赖）
- 架构决策：`docs/adr/0001-纯Rust图像栈.md`




## 语言强制规则（必须严格遵守）

1. 所有回复、解释、对话、说明必须**全程使用中文**，禁止出现任何英文。
2. 不允许切换语言，全程保持纯中文交互。
3. 解释、说明、思考过程、总结、步骤描述，全部使用中文。