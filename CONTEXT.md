# 证件照自动处理工具

一款纯本地离线、高性能、高精度的证件照自动处理工具。底层是一套通用人像处理引擎（抠图、换底、美颜），证件照排版是其中一个核心场景输出。

## Language

**照片 / 图片**:
用户上传的输入素材（自拍、生活照、扫描件），不是独立产品线。
_Avoid_: 相册、素材库

**人像处理**:
对输入人像的通用处理能力，可独立使用：AI 抠图、背景替换、美颜、几何纠偏。
_Avoid_: 图片处理、批量处理

**证件照**:
面向证件规范的成品输出，包含标准尺寸裁剪、底色规范、排版相纸。

**模式套件（Speed / Balanced / Quality）**:
三套预设模型组合，屏蔽底层模型细节，用户按场景一键选择。极速=低配快速、CPU高性能（默认）均衡画质、GPU高质量=极致画质。
_Avoid_: 模型、档位、配置

**姿态纠偏**:
头部姿态角与双肩水平线角度加权融合的旋转校正，配角度阈值保护（±22°）。
_Avoid_: 旋转、扶正

**排版**:
将单张成品按 6 寸 / A4 相纸规格计算行列数拼版输出。

## Relationships

- 一次**照片**输入，经**人像处理**流水线，可产出「通用效果图」与「**证件照**」两类产物
- **证件照**涉及**姿态纠偏**与**排版**；通用效果图只用到**人像处理**前序步骤
- 每个**模式套件**实例映射一组具体模型（人脸检测 / 人体关键点 / 人像分割）

## Example dialogue

> **开发:** 用户上传一张生活照，他能同时下载"白底证件照"和"换底色效果图"两种结果吗？
> **领域专家:** 能。前者走完整流水线含姿态纠偏与排版；后者只到主体抠图+换底为止，这是两条并行出口。
> **开发:** 那"模式套件"是用户切换的还是系统自动的？
> **领域专家:** 用户只按设备场景选 Speed/Balanced/Quality 三选一，具体模型组合是配套预设，用户不可见。

## Flagged ambiguities

- "照片/图片/证件照"曾被认为是三个产品方向 —— 已解决：照片=输入素材，证件照=核心产物，通用人像处理=底层引擎能力